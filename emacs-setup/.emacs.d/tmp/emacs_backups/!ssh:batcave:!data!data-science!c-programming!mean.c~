#include<stdio.h>

int main()
{
  float mean;
  int sum, i;
  int n = 6;
  int a[] = {2,6,7,4,8,9};

  sum = 0;

  for(i = 0 ; i < n, i++)
    {
      sum +=a[i]
    }

  printf("The mean is %f: \n", sum / (float));

  return 0;
  
}
